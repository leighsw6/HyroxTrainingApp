/**
 * Paste your web app config from Firebase Console → Project settings → Your apps → SDK setup.
 * After creating a NEW project + web app, replace every REPLACE_* value below.
 */
window.HYROX_FIREBASE_CONFIG = {
  apiKey: "REPLACE_API_KEY",
  authDomain: "REPLACE_PROJECT_ID.firebaseapp.com",
  projectId: "REPLACE_PROJECT_ID",
  storageBucket: "REPLACE_PROJECT_ID.firebasestorage.app",
  messagingSenderId: "REPLACE_SENDER_ID",
  appId: "REPLACE_APP_ID",
};
